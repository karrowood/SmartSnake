/*const gameArea = document.getElementById('game-area') as HTMLDivElement;
const height = gameArea.offsetHeight;
const width = gameArea.offsetWidth;



//Size 1 (20x20 px)
const columns = Math.floor(width / 20);
const rows = Math.floor(height / 20);
gameArea.style.width = (columns * 20).toString();
gameArea.style.height = (width * 20).toString();

//size 1 (10x10 px)

//size 2 (5x5 px)

//size 3 (2x2 px)
*/